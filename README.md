# portfolio

Personal portfolio for **Nana Baffour Agyemang** — a site reliability engineer.

Static multi-page site: `index.html` (home), `about.html`, `now.html` (journal).

- **Source:** `public/` (HTML, `tokens.css`, `assets/`)
- **Deploy:** Cloudflare Pages (static, no build step). Deploy command: `true`, output directory: `public`.
